import '/components_calendar/hour_background/hour_background_widget.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'background_widget.dart' show BackgroundWidget;
import 'package:flutter/material.dart';

class BackgroundModel extends FlutterFlowModel<BackgroundWidget> {
  ///  State fields for stateful widgets in this component.

  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel1;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel2;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel3;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel4;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel5;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel6;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel7;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel8;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel9;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel10;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel11;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel12;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel13;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel14;
  // Model for HourBackground component.
  late HourBackgroundModel hourBackgroundModel15;

  @override
  void initState(BuildContext context) {
    hourBackgroundModel1 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel2 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel3 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel4 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel5 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel6 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel7 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel8 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel9 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel10 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel11 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel12 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel13 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel14 = createModel(context, () => HourBackgroundModel());
    hourBackgroundModel15 = createModel(context, () => HourBackgroundModel());
  }

  @override
  void dispose() {
    hourBackgroundModel1.dispose();
    hourBackgroundModel2.dispose();
    hourBackgroundModel3.dispose();
    hourBackgroundModel4.dispose();
    hourBackgroundModel5.dispose();
    hourBackgroundModel6.dispose();
    hourBackgroundModel7.dispose();
    hourBackgroundModel8.dispose();
    hourBackgroundModel9.dispose();
    hourBackgroundModel10.dispose();
    hourBackgroundModel11.dispose();
    hourBackgroundModel12.dispose();
    hourBackgroundModel13.dispose();
    hourBackgroundModel14.dispose();
    hourBackgroundModel15.dispose();
  }
}
